app = 'scanner-fly-oz'
primary_region = 'fra'

[build]
  # ничего не указываем — fly сам найдёт main.py и requirements.txt

[http_service]
  internal_port = 8000          # ← ЭТО САМОЕ ГЛАВНОЕ! (у тебя не было)
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
